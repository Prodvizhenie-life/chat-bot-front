import type { FC } from 'react';

import { Page } from '@/shared/ui/page/page';

export const IndexPage: FC = () => {
    return (
        <Page back={false}>
            44
        </Page>
    );
};
